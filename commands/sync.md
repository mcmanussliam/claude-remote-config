---
description: Pull the latest remote config and regenerate all local Claude files
allowed-tools: Bash(claude-remote-config sync*)
---

Sync this project's Claude Remote Config now.

Run the command:

```bash
claude-remote-config sync --project ${CLAUDE_PROJECT_DIR}
```

After it completes, report what was synced using the command's output.
