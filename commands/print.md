---
description: Print a summary of the currently applied remote config
allowed-tools: Bash(claude-remote-config print*)
---

Print a summary of the remote config currently applied to this project.

Run the command:

```bash
claude-remote-config print --project ${CLAUDE_PROJECT_DIR}
```

Present the output to the user.
